func main(i32 int32) (r int32)
{
    i32 = 1 + 1;
    i32 = 1 - 1;
    i32 = 1 * 1;
    i32 = 1 / 1;
    i32 = 1 % 1;
    i32 = 1 & 1;
    i32 = 1 | 1;
    i32 = 1 >> 1;
    i32 = 1 << 1;

    var b bool;

    // TODO: Fix the bug first.
    b = true && true;
    b = true || true;

    b = (i32 < 1);
    b = (1 < 1);
    b = (1 <= 1);
    b = (1 > 1);
    b = (1 >= 1);
    b = (1 == 1);
    b = (1 != 1);
}

main();