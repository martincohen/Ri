var x int32;

for var i int32 = x; i == 0; i = i + x {
    x = i;
}

for var i int32 = x; ; {
    x = i;
}

for var i int32 = x; ; i = i + x {
    x = i;
}

for ; ; x {
    x = x;
}

for ; x == 1; x = x + x {
    x = x;
}

for ; x == 1; {
    x = x;
}

for ; ; {
    x = x;
}

for x == 1 {
    x = x;
}

for {
    x = x;
}
