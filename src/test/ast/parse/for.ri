var x int32;

for var i int32 = x; i; i = i + x {
    x = i;
}

for var i int32 = x; ; {
    x = i;
}

for var i int32 = x; ; i = i + x {
    x = i;
}

for ; ; x {
    x = x;
}

for ; x; x = x + x {
    x = x;
}

for ; x; {
    x = x;
}

for ; ; {
    x = x;
}

for x {
    x = x;
}

for {
    x = x;
}

for var i int32 = x; i; i = i + x {
    x = i;
    continue;
}

for var i int32 = x; i; i = i + x {
    x = i;
    break;
}
